'use strict' // mode strict : é importante para evitar erros, pois com as novas funcionalidades do javascript, pode ser que o código não funcione em navegadores antigos. O modo estrito é uma forma de evitar esses erros.

// company = "Google"; // sem o modo estrito, o código irá funcionar, mas com o modo estrito, o código não irá funcionar, pois a variável não foi declarada.

// console.log(company);

 let company = 'google'

console.log(company)

//isso é uma camada de segurança para o código, pois o modo estrito não permite que o código seja executado se houver algum erro. o global é um objeto que está disponível em todos os arquivos do node, e o company é uma variável que está sendo declarada no arquivo global.



