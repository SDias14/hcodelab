const off =  0
const blackFriday = 1

console.log(Boolean(off)) // toda vez que encontrar um valor 0 ou false, o resultado será false. 

console.log(Boolean(blackFriday)) // toda vez que encontrar um valor diferente de 0 ou false, o resultado será true.

let name

console.log(name)// undefined, pois não foi definido o valor.

let age = null

console.log(age)// null, pois o valor dela é sem valor.