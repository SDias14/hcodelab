/*

let names = ['b', 'c', 'a', 'd', 'e'];

//organizar em ordem alfabetica

names.sort().reverse(); // reverte a ordem

console.log(names.indexOf('a')); // retorna o indice do elemento

*/

//split 

let msg = 'Boas vindas ao curso de javascript';

//acessando a quantidade de palavras

console.log(msg.split(' ')); // retorna um array com as palavras

console.log(msg.split('')); // retorna um array com as letras

msg = msg.split('').reverse() // retorna um array com as palavras e inverte a ordem

console.log(msg);

//join

msg = msg.join(''); // junta as palavras

console.log(msg);


